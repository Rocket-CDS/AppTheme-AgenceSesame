@inherits RocketContentAPI.Components.RocketContentAPITokens<Simplisity.SimplisityRazor>
@AssigDataModel(Model)
@AddProcessDataResx(appThemeAdmin, true)
<!--inject-->
@{
    var info = moduleDataInfo;
    //NOTE: xPath for module settings must use "genxml/settings/*"
}

<div class="w3-row">
    <div class="w3-third w3-padding">
        <label>@ResourceKey("SecureHoliday.clientnumber")</label>
        @TextBox(info, "genxml/settings/clientnumber", "class='w3-input w3-border'", "")
    </div>
    <div class="w3-third w3-padding">
        <label>@ResourceKey("DNNrocket.cssClass")</label>
        @TextBox(info, "genxml/settings/cssclass", "class='w3-input w3-border'", "")
    </div>
    <div class="w3-row w3-padding">
        <label>TEST Mode (Show redirect URL, do NOT redirect)</label><br/>
        @CheckBox(info, "genxml/settings/testmode",""," class='w3-check'")
    </div>
</div>
