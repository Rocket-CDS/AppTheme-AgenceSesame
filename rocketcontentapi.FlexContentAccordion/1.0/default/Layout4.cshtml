@inherits RocketContentAPI.Components.RocketContentAPITokens<Simplisity.SimplisityRazor>
@using DNNrocketAPI.Components;
@AssignDataModel(Model)
@AddProcessDataResx(appThemeView, true)
<!--inject-->

@{
    var contentId = "content" + articleRowData.RowKey + sessionParams.ModuleId;
    var defaultOpenParentClass = "";
    var defaultOpenClass = "";
    var defaultOpenStyle = "display:none;";
    if (articleRowData.GetBool("genxml/checkbox/defaultopen"))
    {
        defaultOpenParentClass = "flexcontent-active";
        defaultOpenClass = "fc-active" + sessionParams.ModuleId;
        defaultOpenStyle = "";
    }
}

<div class="flexcontent-div @(defaultOpenParentClass) @rowData.GetXmlProperty("genxml/textbox/cssclass")">
    <div class="fca-header" onclick="slideAccordionFC('@(contentId)','@(sessionParams.ModuleId)','@(moduleData.GetSetting("disableaccordianmode"))')" style="cursor:pointer;">[INJECT:appthemeview,ArticleRowHeaderView.cshtml]</div>
<div id="@(contentId)" class="fca-content fcd-flex @(defaultOpenClass)" style="@(defaultOpenStyle)">
    <div class="flexcontent-text">
        @HtmlOf(info.GetXmlProperty("genxml/lang/genxml/textbox/richtext"))
        @HtmlOf(info.GetXmlProperty("genxml/lang/genxml/textbox/htmltext"))
        @if (articleRowData.GetLinkList().Count() > 0) {
        <div class="newspaper-link">
                [INJECT:appthemeview,Links.cshtml]
            </div>
        }
        @if (articleRowData.GetDocList().Count() > 0) {
        <div class="documents">
            [INJECT:appthemeview,Documents.cshtml]
        </div>
        }
    </div>
    <div class="flexcontent-media">
        <div class="fcd-images ">
            @foreach (ArticleImage articleImage in articleRowData.GetImages())
            {
                if (articleImage.RelPath != "")
                {
                    if (rowData.GetXmlPropertyBool("genxml/checkbox/openfancybox") && !moduleData.ECOMode)
                    {
                        <div>
                            <a data-fancybox="@(articleRowData.RowKey)" data-caption="@(articleImage.Alt)" href="@(ImageUrl(articleImage.RelPath,0,0))">
                                <img src="@ImageUrl(articleImage.RelPath, articleImage.Width, articleImage.Height)" alt="@(articleImage.Alt)" />
                            </a>
                        </div>
                    }
                    else
                    {
                        <div><img src="@ImageUrl(articleImage.RelPath, articleImage.Width, articleImage.Height)" alt="@(articleImage.Alt)" /></div>
                    }
                }
            }
        </div>
        @if (rowData.GetXmlProperty("genxml/lang/genxml/textbox/video") != "")
        {
            <div class="fcd-video"><div class="video-container">@HtmlOf(rowData.GetXmlProperty("genxml/lang/genxml/textbox/video"))</div></div>
        }
    </div>
</div>
</div>