@inherits RocketContentAPI.Components.RocketContentAPITokens<Simplisity.SimplisityRazor>
@using DNNrocketAPI;
@using Simplisity;
@using RocketContentAPI.Components;
@using DNNrocketAPI.Components;
@using RocketPortal.Components;

<!-- TODO Rendre ça plus propre - système D pour que ça marche -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/fancybox/3.2.5/jquery.fancybox.min.css" />
<script src="https://cdnjs.cloudflare.com/ajax/libs/fancybox/3.2.5/jquery.fancybox.min.js"></script>

@{
    var articleData = (ArticleLimpet)Model.GetDataObject("articledata");
    var moduleData = (ModuleContentLimpet)Model.GetDataObject("modulesettings");
}

@foreach (var articleRowData in articleData.GetRows())
{
    var info = articleRowData.Info;
    <div class="@moduleData.Record.GetXmlProperty("genxml/settings/cssclass")">
        @if (!info.GetXmlPropertyBool("genxml/checkbox/hiderow"))
        {
        <div class="gallery-wrapper">
            @if (info.GetXmlProperty("genxml/textbox/title") != "")
            {
                <h2>@info.GetXmlProperty("genxml/textbox/title")</h2>
            }
            <hr />
            @{
                var datalist = articleRowData.GetImageList();
                var index = 0;
                var group = articleRowData.RowKey;
                var width = moduleData.Record.GetXmlPropertyInt("genxml/settings/width");
                var height = moduleData.Record.GetXmlPropertyInt("genxml/settings/height");
            }
            <div class="gallery">
                @foreach (var imgInfo in datalist)
                {
                    index++;
                    var articleImage = new ArticleImage(imgInfo, "articleimage");
                    var alt = info.GetXmlProperty("genxml/imagelist/genxml[" + @index + "]/hidden/imagealtarticleimage");
                    if (articleImage.RelPath != "")
                    {
                        <a data-fancybox="@(group)" data-caption="@(alt)" href="@(articleImage.RelPath)">
                            <img src="@ThumbnailImageUrl(articleImage.RelPath, width, height)" alt="@(alt)" />
                        </a>
                    }
                }
            </div>
        </div>
        }
    </div>
}

<script type="text/javascript">

    $("[data-fancybox]").fancybox({
        lang: 'fr',
        i18n: {
            'fr': {
                CLOSE: 'Fermer',
                NEXT: 'Suivant',
                PREV: 'Précédent',
                ERROR: 'Le contenu demandé ne peut pas être chargé.<br />Veuillez réessayer plus tard.',
                PLAY_START: 'Démarrer le diaporama',
                PLAY_STOP: 'Mettre en pause le diaporama',
                FULL_SCREEN: 'Plein écran',
                THUMBS: 'Vignettes',
                DOWNLOAD: 'Télécharger',
                SHARE: 'Partager',
                ZOOM: 'Zoomer'
            }
        }
    });

</script>


