@inherits RocketContentAPI.Components.RocketContentAPITokens<Simplisity.SimplisityRazor>
@AssigDataModel(Model)
@AddProcessDataResx(appThemeView, true)
<!--inject-->

@{
   
    var columns = "";
    if (moduleData.GetSetting("columns") != "default")
    {
        columns = moduleData.GetSetting("columns");
    }

    var cssClass = "";
    if (moduleData.GetSetting("cssclass") != "")
    {
        cssClass = "class='" + moduleData.GetSetting("cssclass") + "' ";
    }

}

<div @(cssClass)>

    <div class="flexcontent-wrapper @(columns)">

        @foreach (var articleRowData in articleData.GetRows())
        {
            Model.SetDataObject("articlerow", articleRowData);
            var info = articleRowData.Info;
            var layout = info.GetXmlPropertyInt("genxml/select/layout");
            var hideRow = info.GetXmlPropertyBool("genxml/checkbox/hiderow");

            if (!hideRow)
            {
                @RenderTemplate("layout" + layout + ".cshtml", moduleData.ModuleRef, appThemeView, Model, true)
            }
        }
    
    </div>

</div>

<script type="text/javascript">

    $("[data-fancybox]").fancybox({
        lang: 'fr',
        i18n: {
            'fr': {
                CLOSE: 'Fermer',
                NEXT: 'Suivant',
                PREV: 'Précédent',
                ERROR: 'Le contenu demandé ne peut pas être chargé.<br />Veuillez réessayer plus tard.',
                PLAY_START: 'Démarrer le diaporama',
                PLAY_STOP: 'Mettre en pause le diaporama',
                FULL_SCREEN: 'Plein écran',
                THUMBS: 'Vignettes',
                DOWNLOAD: 'Télécharger',
                SHARE: 'Partager',
                ZOOM: 'Zoomer'
            }
        }
    });

</script>

