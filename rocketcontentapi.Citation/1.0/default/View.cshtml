@inherits RocketContentAPI.Components.RocketContentAPITokens<Simplisity.SimplisityRazor>
@AssignDataModel(Model)
@AddProcessDataResx(appThemeView, true)
<!--inject-->

@{
    var openDiv = "";
    var closeDiv = "";
    if (moduleData.GetSetting("cssclass") != "")
    {
        openDiv = "<div class='" + moduleData.GetSetting("cssclass") + "'>";
        closeDiv = "</div>";
    }
}

@HtmlOf(openDiv)
    <div class="citation-window">   
        <div class="citation-content">
        @foreach (var articleRowData in articleData.GetRows())
        {
            if (!articleRowData.GetBool("genxml/checkbox/hiderow"))
            {
                <h2>@rowData.GetXmlProperty("genxml/lang/genxml/textbox/title")</h2>
                <hr />
                <div class="citation-text">@BreakOf(rowData, "genxml/lang/genxml/textbox/content")</div>
                var linkData = articleRowData.Getlink(0);
                if (!linkData.Hidden && !linkData.IsDisabled)
                {
                    <div class="citation-link"><a class="bigbutton" href="@(linkData.Url)" target="@(linkData.Target)">@linkData.Name</a></div>
                }
            }
        }
        </div>
    </div>
@HtmlOf(closeDiv)
