@inherits RocketContentAPI.Components.RocketContentAPITokens<Simplisity.SimplisityRazor>
@AssignDataModel(Model)
@AddProcessDataResx(appThemeAdmin, true)
<!--inject-->
@{
    var info = moduleDataInfo;
    var mask = "";
    if (rowData.GetXmlPropertyBool("genxml/checkbox/mask"))
    {
        mask = "tih-mask";
    }
    var articleImage = articleRowData.GetImage(0);
    var width = moduleData.GetSettingInt("width");
    var height = moduleData.GetSettingInt("height");
    var separationLine = moduleData.GetSettingBool("separation");
    var crop = rowData.GetXmlProperty("genxml/select/crop");
    if (crop == "")
    {
        crop = "center";
    }
    var openDiv = "";
    var closeDiv = "";
    if (moduleData.GetSetting("cssclass") != "")
    {
        openDiv = "<div class='" + moduleData.GetSetting("cssclass") + "'>";
        closeDiv = "</div>";
    }
}

@HtmlOf(openDiv)
    <div class="topimageheader @(mask)" style="background:url('@ImageUrl(articleImage.RelPath, width, height)') center @(crop) no-repeat;background-size:cover;">
        <div class="caption">
            @if (rowData.GetXmlProperty("genxml/lang/genxml/textbox/title") != "")
            {
                <div class="h2-headline">@rowData.GetXmlProperty("genxml/lang/genxml/textbox/title")</div>
            }
            @if (separationLine)
            {
                <hr />
            }
            @if (rowData.GetXmlProperty("genxml/lang/genxml/textbox/text") != "")
            {
                <div class="h3-headline">@rowData.GetXmlProperty("genxml/lang/genxml/textbox/text")</div>
            }
        </div>
    </div>
@HtmlOf(closeDiv)