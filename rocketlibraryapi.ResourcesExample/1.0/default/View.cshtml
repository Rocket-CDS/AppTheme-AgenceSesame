@inherits RocketDirectoryAPI.Components.RocketDirectoryAPITokens<Simplisity.SimplisityRazor>
@using RocketDirectoryAPI.Components;
@using DNNrocketAPI;
@using Simplisity;
@using RocketPortal.Components;
@using DNNrocketAPI.Components;
@using System.Globalization;
@using Rocket.AppThemes.Components;

<div id="rocketlibrarylist" class="">
    @{
        Model.SetSetting("searchview", "false");
        var appTheme = (AppThemeLimpet)Model.GetDataObject("apptheme");
        var articleData = (ArticleLimpet)Model.GetDataObject("articledata");
        if (articleData != null)
        {
            @RenderTemplate("ArticleDetail.cshtml", appTheme, Model, true)
        }
        else
        {
            @RenderTemplate("ArticleList.cshtml", appTheme, Model, true)
        }
    }
</div>

<div class="simplisity_loader">
    <span class=" simplisity_loader_inner">
    </span>
</div>
