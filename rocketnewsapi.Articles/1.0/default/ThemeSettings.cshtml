@inherits RocketDirectoryAPI.Components.RocketDirectoryAPITokens<Simplisity.SimplisityRazor>
@AssigDataModel(Model)
@AddProcessDataResx(appTheme, true)
<!--inject-->
@{
    var info = moduleDataInfo;
    //NOTE: xPath for module settings must use "genxml/settings/*"
}

<div class="w3-row">
    <div class="w3-quarter w3-padding">
        <label>@ResourceKey("DNNrocket.width") (List)</label>
        @TextBox(info, "genxml/settings/widthlist", "type='number' class='w3-input w3-border'", "200")
    </div>
    <div class="w3-quarter w3-padding">
        <label>@ResourceKey("DNNrocket.height") (List)</label>
        @TextBox(info, "genxml/settings/heightlist", "type='number' class='w3-input w3-border'", "150")
    </div>
    <div class="w3-quarter w3-padding">
        <label>@ResourceKey("DNNrocket.rowlimit")</label>
        @TextBox(info, "genxml/settings/limit", "type='number' class='w3-input w3-border'", "0")
    </div>
</div>
<div class="w3-row">
    <div class="w3-quarter w3-padding">
        <label>@ResourceKey("DNNrocket.width") (Detail)</label>
        @TextBox(info, "genxml/settings/widthdetail", "type='number' class='w3-input w3-border'", "400")
    </div>
    <div class="w3-quarter w3-padding">
        <label>@ResourceKey("DNNrocket.height") (Detail)</label>
        @TextBox(info, "genxml/settings/heightdetail", "type='number' class='w3-input w3-border'", "400")
    </div>
</div>
